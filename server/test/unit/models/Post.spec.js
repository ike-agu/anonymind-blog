// const Post = require('../models/Post.spec')
// const pg = require('pg')
// jest.mock('pg');

// const db = require('../../../dbConfig/init');

// describe('user', () =>{
//   beforeEach(() => jest.clearAllMocks())
//   afterAll(() => jest.resetAllMocks())

//   describe('all', () => {
//     test('should return all users successfully from db', async() =>{
//       jest.spyOn(db, 'query')
//            .mockResolvedValueOnce({rows: [ {},{},{},{},{}]})
//            const all = await User.all
//            expect(all).toHaveLength(5)
//     })
//   });

// })
